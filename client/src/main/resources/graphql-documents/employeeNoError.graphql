query employeeNoError($id: ID!) {
    employee(id: $id) {
        firstName
        lastName
        salary
        position
        age
        passport {
            code
            number
        }
    }
}
